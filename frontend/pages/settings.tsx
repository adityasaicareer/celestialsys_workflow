import Layout from '../components/Layout';
import ProtectedRoute from '../components/ProtectedRoute';
import { useState } from 'react';

export default function Settings() { const [muted, setMuted] = useState(false); const [saved, setSaved] = useState(false); return <ProtectedRoute><Layout title="Settings" subtitle="Configure your organization and notification preferences."><div className="settings-grid"><section className="panel settings-section"><span className="eyebrow">ORGANIZATION</span><h2>Organization profile</h2><p>These details appear on visitor passes and email notifications.</p><label>Organization name<input defaultValue="Acme Corporation" /></label><label>Default timezone<select defaultValue="Asia/Kolkata"><option value="Asia/Kolkata">India Standard Time (IST)</option><option>Pacific Standard Time (PST)</option></select></label><label>Visitor pass prefix<input defaultValue="ACME" /></label></section><section className="panel settings-section"><span className="eyebrow">NOTIFICATIONS</span><h2>Internet access alerts</h2><p>Control when security teams are notified about visitor internet requests.</p><div className="setting-row"><div><strong>Internet access requested</strong><small>Notify approvers when a visitor needs internet access.</small></div><button className={`toggle ${!muted ? 'on' : ''}`} aria-label="Toggle internet notifications" onClick={() => setMuted(!muted)}><span /></button></div><div className="setting-row"><div><strong>Daily access summary</strong><small>Send a daily summary to Super Admins.</small></div><button className="toggle on" aria-label="Toggle daily summary"><span /></button></div><div className="muted-box">{muted ? 'Internet-access notifications are muted.' : 'Notifications are currently enabled for all approvers.'}</div></section></div><div className="form-actions settings-actions">{saved && <div className="success-message">Settings saved successfully.</div>}<button className="button primary" onClick={() => { setSaved(true); setTimeout(() => setSaved(false), 2500); }}>Save changes</button></div></Layout></ProtectedRoute>; }
