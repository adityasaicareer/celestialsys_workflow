import { useState } from 'react'; import ProtectedRoute from '../components/ProtectedRoute';
export default function Settings(): JSX.Element { const [saved,setSaved]=useState(false); const [prefs,setPrefs]=useState({email:true,sms:false,approval:true,checkout:true}); return <ProtectedRoute><div className="mx-auto max-w-2xl space-y-5"><h2 className="text-2xl font-bold">Settings</h2><section className="card"><h3 className="text-lg font-semibold">Notification preferences</h3><div className="mt-4 space-y-4">{Object.entries(prefs).map(([key,value])=><label key={key} className="flex items-center justify-between border-b pb-3 text-sm"><span className="capitalize">{key} notifications</span><input type="checkbox" checked={value} onChange={e=>setPrefs(p=>({...p,[key]:e.target.checked}))} aria-label={`${key} notifications`} /></label>)}</div></section><section className="card"><h3 className="text-lg font-semibold">Organization configuration</h3><label className="label mt-4 block" htmlFor="org">Organization name</label><input id="org" className="field" defaultValue="Acme Corp" /><label className="label mt-4 block" htmlFor="passes">Available pass types</label><input id="passes" className="field" defaultValue="Standard, VIP, Contractor, Temporary" /></section><button className="btn-primary" onClick={()=>setSaved(true)}>Save settings</button>{saved&&<p role="status" className="text-sm text-green-700">Settings saved successfully.</p>}</div></ProtectedRoute>; }