import { useState } from 'react'; import { useRouter } from 'next/router'; import { useAuth } from '../components/AuthProvider'; import FormField from '../components/FormField';
function LoginPage(): JSX.Element { const { login } = useAuth(); const router = useRouter(); const [email, setEmail] = useState('alex@example.com'); const [password, setPassword] = useState('password'); const submit = (e: React.FormEvent) => { e.preventDefault(); login({ id: '1', name: 'Alex Morgan', email, role: 'admin', organization: 'Acme Corp', active: true }); router.push('/dashboard'); }; return <main className="flex min-h-screen items-center justify-center bg-slate-950 p-4"><form onSubmit={submit} className="card w-full max-w-md"><h1 className="text-2xl font-bold">Sign in to VisitorHub</h1><p className="mt-2 text-sm text-slate-600">Demo authentication is enabled.</p><div className="mt-6 space-y-4"><FormField label="Email" name="email" type="email" value={email} onChange={setEmail} required /><FormField label="Password" name="password" type="password" value={password} onChange={setPassword} required /></div><button className="btn-primary mt-6 w-full" type="submit">Sign in</button></form></main>; } LoginPage.displayName = 'LoginPage'; export default LoginPage;